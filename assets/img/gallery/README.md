# Скриншоты для галереи

Сюда складывайте **скриншоты из игры** — в галерее на главной показываются только они.

## Как добавить фото

1. Положите файл в эту папку, например `screenshot-1.avif`.
2. Откройте `assets/js/site-data.js` и добавьте запись в массив `gallery`:

```js
gallery: [
  {
    src: "assets/img/gallery/screenshot-1.avif",
    wide: false,                                // широкая плитка (занимает 2 колонки)
    tall: false,                                // высокая плитка (занимает 2 ряда)
    ru: { caption: "Подпись на русском" },
    en: { caption: "Caption in English" }
  },
  // ...
]
```

3. Если галерея пустая — весь раздел на главной автоматически скрывается.

## Рекомендации

- Формат: **.avif** (легче и быстрее грузится).
- Ширина: **1600 px** достаточно (лайтбокс показывает до 1240 px). Файл можно не сжимать вручную.
- Для широких кадров ставьте `wide: true`, для вертикальных — `tall: true`.
- Сжимайте картинки перед добавлением (например, Squoosh / `cwebp -q 78`). Адаптивные варианты `-640.avif` / `-960.avif` и запись в `assets/js/media-manifest.js` можно сделать по желанию: без них галерея просто покажет исходный файл.

Картинки для статей вики (карты эпох, территории фракций, раса Вастт) лежат отдельно — в `assets/img/lore/`.
