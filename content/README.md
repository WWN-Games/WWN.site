# Как добавлять статьи в вики

Статья — обычный Markdown-файл. Файлы лежат в `content/ru/<раздел>/` и `content/en/<раздел>/`, структура папок в обоих языках совпадает. **Папка = раздел вики** (`content/ru/lore/…` попадает в раздел «Лор»), список разделов — в `data/wiki-sections.json`.

## Новая статья за два шага

1. Создайте файл в папке раздела, например `content/ru/guides/new-guide.md`, и такой же на втором языке — `content/en/guides/new-guide.md`.
2. Заполните шапку в начале файла:

   ```md
   ---
   title: "Название статьи"
   desc: "Одна строка для меню, поиска и описания страницы."
   order: 3
   updated: 2026-09-15
   draft: true
   ---
   ```

Затем соберите вики из корня репозитория:

```bash
node tools/build-wiki.mjs
```

Сборка сама поставит статью в меню, поиск и `sitemap.xml` и сделает страницу с адресом `/wiki/guides/new-guide/`. Проверка без записи — `node tools/build-wiki.mjs --check`.

## Шапка (front matter)

| Поле | Что делает |
| --- | --- |
| `title` | Заголовок статьи, **всегда в двойных кавычках**. В тексте `#` писать не нужно |
| `desc` | Короткое описание для меню, поиска и поисковой выдачи, **тоже всегда в кавычках** |
| `order` | Порядок внутри раздела (1, 2, 3…). Одинаковые значения — ошибка сборки |
| `updated` | Дата в формате `ГГГГ-ММ-ДД`, выводится в подписи статьи |
| `draft: true` | Черновик: скрыт из меню, поиска, хаба и sitemap, но открывается по прямой ссылке. Готово — уберите строку |

Правило простое: **`title` и `desc` — всегда в двойных кавычках** (тогда двоеточия, решётки и другие символы внутри безопасны для YAML, который читают редакторы; сайт кавычки снимает). Число, дата и `draft` пишутся без кавычек. Если внутри нужна двойная кавычка — возьмите значение в одинарные; сборка проверит и подскажет.

Статью ищут по папке раздела и `order`, регистрировать её больше нигде не нужно.

## Текст и оглавление

- `##` и `###` автоматически попадают в оглавление (если заголовков меньше трёх, оглавление не выводится).
- Обычный Markdown: списки, **жирный**, таблицы — оформление подставляется сайтом.

## Ссылки и источники

- **На другую статью:** `[текст](overview.md)` или с указанием папки `[текст](../factions/fenearth.md)` — сайт сам превратит их в правильный адрес `/wiki/factions/fenearth/`.
- **На заголовок внутри статьи:** `[текст](#имя-заголовка)` — имя совпадает с id заголовка (например, `#карта-галактики`). Все заголовки `##`/`###` получают id автоматически.
- **На внешний источник:** обычная ссылка `[текст](https://example.org)` — она откроется в новой вкладке и получит пометку «↗».
- **На статью, которой ещё нет:** ссылка автоматически станет «красной» с подсказкой «Статья ещё не написана».
- **На файлы:** пути пишутся так, как если бы статья лежала в `/wiki/`, — например `../assets/img/lore/tc-ru.avif`. Сайт сам пересчитает их под глубину страницы.

### Блок «Источники» / «См. также»

В конце статьи можно добавить компактный блок со ссылками на источники или связанные статьи. Достаточно обычного заголовка с одним из названий — оформление подставится само:

```md
## Источники

- [Steam Workshop: мод WWN](https://steamcommunity.com/sharedfiles/filedetails/?id=3247893564)
- [GitHub: сообщить об ошибке](https://github.com/WWN-Games/WWN.issues/issues/new/choose)
- [История галактики](history.md)
```

Распознаются заголовки: **Источники**, **Ссылки**, **Внешние ссылки**, **См. также** (в EN — **Sources**, **Links**, **External links**, **See also**, **References**). Списки под таким заголовком оформляются компактнее, с линией-разделителем; внешние ссылки в них получают «↗», внутренние работают как обычные ссылки по вики.

### Сноски

В тексте можно ставить сноски — они становятся верхними индексами `[1]`, а внизу статьи появляется блок «Примечания» с обратными ссылками «↑»:

```md
Протонская армия формировалась в Территории Отен[^1], а её флот — на верфях Орбитальной суперверфи[^2].

[^1]: Подробнее — в статье [Движение Протон](proton.md).
[^2]: Данные с [сайта мода](https://example.org) на момент 3-й эпохи.
```

- Номер присваивается по порядку первого упоминания; повторная ссылка с тем же именем (`[^1]` дважды) ведёт на то же примечание.
- Внутри примечания работает обычный markdown: **жирный**, ссылки на статьи (`.md`) и внешние источники (получат «↗»).
- Имя сноски (`1`, `note`, `steam`) — любое, главное чтобы совпадало в тексте и в определении; определения пишутся отдельными строками в конце статьи.
- Блок «Примечания» автоматически добавляется в оглавление.

## Картинки

Картинка отдельным абзацем превращается в фигуру с подписью из alt:

```md
![Карта галактики в эпоху Тройственного Конфликта](../assets/img/lore/tc-ru.avif)
```

Несколько картинок в одном абзаце собираются в галерею.

Картинки статей кладите в `assets/img/lore/` (не в `assets/img/gallery/` — там только галерея главной). Подробнее — `../assets/README.md`.

## Особые блоки

- Аудио (гимны, примеры речи):

  ```html
  <div class="audio-card"><span class="audio-card__label">Гимн</span><audio controls preload="metadata" src="../assets/audio/fenearth-anthem.mp3"></audio></div>
  ```

- Выноски: `<div class="callout">текст</div>` и `<div class="callout callout--warn">текст</div>`.

## Два языка

- Статья должна существовать в обеих папках с одинаковым slug — сборка проверит это и не даст забыть перевод.
- Внутри данных (`data/`) и подписей, если `ru` и `en` совпадают (например, имя собственное), `en` можно не писать — подставится русское значение.
- **Русский текст — источник истины.** Если формулировки в языках расходятся, английский переводится с русского, а не наоборот.

## Терминология (RU → EN)

Единые варианты имён и терминов — используйте их во всех статьях и файлах `data/`:

| Русский | English |
| --- | --- |
| Фенземская Республика (ФЗР/Фензем) | Fenearth Republic (FR/Fenearth) |
| Движение Протон | Proton Movement |
| Единая Протонская Освободительная Армия | United Proton Liberation Army |
| Вастт, протонцы | Vasst, Protonians |
| Дегеры | Deguers |
| Всегалактическая Федерация (ВГФ) | Galactic Federation (GF) |
| Территория Отен | Oten Territory |
| Капо-Нантская / Капо-Бульская часть галактики | Capo-Nant / Capo-Bul sector of the galaxy |
| Минэль, минельцы | Minel, Minelians |
| Зиёнгоны | Ziyongons |
| Странники / Хранители | Wanderers / Keepers |
| Тройственный Конфликт | Triple Conflict |
| Война Очищения | War of Purification |
| Галактическая Смута | Galactic Turmoil |
| Период Затишья | Period of Calm |
| Период Гийи | Period of Gii |
| Далекое Прошлое | Distant Past |
| Территория Черного Рынка | Black Market Territory |
| Инкорпорация Независимых Регионов | Incorporation of Independent Regions |

Аббревиатуры без расшифровки пишутся в EN так же, как в игре: СИП → TIC, КЗС → DSC, МПВО → MAD, Дальнострел → Eye of Reach, СЛУК → SLSS.

## Как посмотреть локально

Сначала соберите вики — меню, поиск и страницы статей:

```bash
node tools/build-wiki.mjs
```

Сайт использует ES-модули и `fetch()`, поэтому открывать его нужно через локальный сервер:

```bash
python3 -m http.server 8000
```

и зайти на <http://localhost:8000>.
